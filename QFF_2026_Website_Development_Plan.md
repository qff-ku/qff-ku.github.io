# Qiskit Fall Fest 2026 웹사이트 개발 계획

## 1. 프로젝트 개요

- 프로젝트명: Qiskit Fall Fest 2026 공식 안내·홍보 웹사이트
- 운영 주체: Qiskit Fall Fest 2026 학교 단위 운영진
- 사이트 주소: `https://qff-ku.github.io`
- 목적: 행사 소개, 일정·장소 안내, 참가 방법 전달, 홍보 콘텐츠 제공
- 운영 형태: 별도 서버나 데이터베이스가 없는 정적 웹사이트
- 지원 언어: 한국어·영어
- 초기 대상: 2026 행사
- 장기 방향: 추후 Qiskit Fall Fest 2027이 개최될 경우 동일한 저장소에 연도별 페이지 추가

사이트의 주된 기능은 정보를 보여주는 것이다. 회원가입, 로그인, 결제, 자체 신청 시스템, 관리자 페이지는 만들지 않는다. 참가 신청이 필요하면 Google Forms 등 외부 서비스를 연결한다.

## 2. GitHub 운영 구조

### 2.1 계정과 소유권

개인 GitHub 계정으로 로그인한 뒤 무료 GitHub Organization `qff-ku`를 생성한다. Organization은 별도 로그인 계정이 아니라 여러 개인 계정이 함께 관리하는 작업 공간이다.

- Organization: `qff-ku`
- 저장소: `qff-ku/qff-ku.github.io`
- 공개 주소: `https://qff-ku.github.io`
- 저장소 공개 범위: Public
- 기본 브랜치: `main`

단, `qff-ku`라는 Organization 이름이 이미 사용 중이면 다른 이름을 정해야 한다. 사이트 주소는 실제 Organization 이름에 따라 결정된다.

### 2.2 권한 구성

- Organization Owner: 최소 2명
- Maintain: 웹사이트 실무 책임자
- Write: 코드·문구·이미지를 수정하는 운영진
- Read: 검토만 필요한 구성원

개인 계정에 저장소를 직접 소유시키지 않고 Organization이 소유하게 한다. 담당자가 바뀌면 후임자를 먼저 Owner로 추가하고 정상 접근을 확인한 뒤 전임자의 권한을 제거한다.
브랜치 보호와 Pages 설정처럼 관리자 권한이 필요한 작업은 Organization Owner 또는 저장소 Admin이 담당한다.

## 3. 기술 구성

### 3.1 권장 기술

- 프레임워크: Astro
- 언어: TypeScript, HTML, CSS
- 결과물: 완전히 정적인 HTML·CSS·JavaScript
- 배포: GitHub Actions → GitHub Pages
- 콘텐츠 관리: 연도·언어별 TypeScript 또는 JSON 데이터 파일
- 외부 신청: Google Forms 링크

Astro를 권장하는 이유는 다음과 같다.

- 행사 사이트에 필요한 정적 페이지를 가볍게 생성할 수 있다.
- 헤더, 일정표, 연사 카드, 장소 안내 등 반복 요소를 컴포넌트로 재사용할 수 있다.
- 한국어와 영어 페이지가 같은 디자인을 공유할 수 있다.
- 2027 페이지를 추가할 때 기존 구조를 그대로 복제·확장할 수 있다.
- Codex가 구조를 이해하고 수정하기 쉽다.
- 필요하지 않은 클라이언트 JavaScript를 최소화할 수 있다.

React 기반 웹 애플리케이션, 데이터베이스, 별도 백엔드 서버, WordPress는 사용하지 않는다. 이번 사이트의 범위에는 과도하다.

초기 버전은 별도 CMS, 다국어 플러그인, UI 컴포넌트 라이브러리를 추가하지 않는다. Astro의 파일 기반 라우팅, HTML, CSS와 소량의 TypeScript만 사용하고 실제 필요가 확인될 때만 의존성을 추가한다.

## 4. URL 및 연도별 구조

처음부터 연도와 언어가 URL에 나타나도록 구성한다.

- `https://qff-ku.github.io/` → 현재 행사 한국어 페이지로 이동
- `https://qff-ku.github.io/2026/ko/` → 2026 한국어 페이지
- `https://qff-ku.github.io/2026/en/` → 2026 영어 페이지

2027 행사가 추가되면 다음과 같이 확장한다.

- `https://qff-ku.github.io/2027/ko/`
- `https://qff-ku.github.io/2027/en/`
- 루트 주소 `/`의 이동 대상만 2027 한국어 페이지로 변경
- 2026 페이지는 그대로 보존

한국어와 영어 페이지 사이에는 항상 언어 전환 버튼을 제공한다. 자동 번역 기능은 사용하지 않고, 검수된 한국어·영어 원문을 각각 저장한다.

### 4.1 Astro URL 설정

`astro.config.mjs`에는 다음 원칙을 적용한다.

- `site`: `https://qff-ku.github.io` 또는 실제 생성된 Organization 이름의 GitHub Pages 주소
- `base`: 설정하지 않음. 저장소 이름을 실제 Organization 이름과 같은 `<organization>.github.io`로 만들기 때문이다.
- `trailingSlash`: `always`
- `/` 리디렉션: Astro의 `redirects` 설정으로 `/2026/ko/`를 지정

GitHub Pages의 정적 사이트에서는 루트 이동이 서버의 HTTP 301이 아니라 생성된 HTML의 클라이언트 리디렉션으로 동작한다. 검색엔진 중복 인식을 막기 위해 한국어·영어 본문 페이지에 각각 올바른 canonical URL을 지정한다.

## 5. 페이지 정보 구조

2026 페이지는 한 페이지형 랜딩 페이지를 기본으로 한다. 상단 메뉴를 누르면 해당 구역으로 이동한다.

1. 헤더
   - Qiskit Fall Fest 로고 또는 워드마크
   - 행사 소개, 프로그램, 장소, FAQ 메뉴
   - 한국어·English 전환 버튼
   - 참가 신청 버튼

2. 메인 화면
   - Qiskit Fall Fest 2026 공식 행사명
   - 핵심 슬로건 또는 주제
   - 날짜와 장소
   - 참가 신청 또는 자세히 보기 버튼
   - 대표 이미지 또는 행사 키비주얼

3. 행사 소개
   - Qiskit Fall Fest가 무엇인지
   - 2026 행사의 목적과 주제
   - 주요 참가 대상

4. 프로그램
   - 시간대별 일정
   - 세션명과 간단한 설명
   - 연사·진행자 정보
   - 아직 확정되지 않은 정보는 `추후 공개`로 표시

5. 참가 안내
   - 신청 기간
   - 참가비 여부
   - 참가 조건
   - Google Forms 등 외부 신청 링크
   - 신청 마감 상태 표시

6. 장소 안내
   - 건물명과 상세 주소
   - 대중교통·도보 안내
   - 지도 링크
   - 교내 행사장 사진 또는 약도

7. FAQ
   - 참가 자격
   - 준비물
   - 신청 변경·취소
   - 언어 지원
   - 문의 방법

8. 주최·주관·후원
   - 학교 및 관련 기관 명칭
   - 공식 로고
   - 명칭과 로고의 사용 승인을 확인한 자료만 게시

9. 문의 및 푸터
   - 공식 문의 이메일
   - 공식 SNS 링크
   - 저작권 표시
   - 개인정보 처리 관련 안내가 필요한 외부 서비스 링크

행사 정보가 많아지면 프로그램과 연사 소개만 별도 페이지로 분리할 수 있다. 초기 버전은 한 페이지 구성이 관리하기 쉽다.

## 6. 권장 코드 구조

```text
qff-ku.github.io/
├─ .github/
│  └─ workflows/
│     └─ deploy.yml
├─ public/
│  ├─ favicon.svg
│  └─ images/
│     └─ 2026/
│        └─ ...
├─ src/
│  ├─ components/
│  │  └─ EventPage.astro
│  ├─ data/
│  │  └─ 2026.ts
│  ├─ pages/
│  │  ├─ 404.astro
│  │  └─ 2026/
│  │     ├─ ko.astro
│  │     └─ en.astro
│  └─ styles/
│     └─ global.css
├─ astro.config.mjs
├─ package.json
└─ README.md
```

한국어·영어 페이지는 같은 `EventPage.astro`를 사용하고 `data/2026.ts`의 언어별 콘텐츠만 전달한다. 섹션이 실제로 복잡해지거나 반복될 때만 별도 컴포넌트로 분리한다. 협업 절차는 초기에는 README에 함께 기록한다.

행사 데이터에는 다음 상태를 둔다.

- `upcoming`: 신청 전
- `open`: 신청 가능
- `closed`: 신청 마감
- `ended`: 행사 종료

`registrationUrl`은 선택값으로 두고, 상태에 따라 신청 버튼과 안내 문구가 함께 변경되게 한다. 이를 통해 마감 또는 종료 후에도 잘못된 신청 링크가 노출되는 일을 막는다.

## 7. 디자인 방향

- 모바일 우선 반응형 디자인
- 행사 공식 색상과 키비주얼을 중심으로 구성
- 첫 화면에서 날짜·장소·신청 버튼을 즉시 확인 가능
- 장식보다 행사 정보 전달과 가독성을 우선
- 한국어와 영어에서 레이아웃이 깨지지 않도록 여유 있는 폭 사용
- 본문과 배경의 명암 대비 확보
- 본문 바로가기 링크와 명확한 키보드 포커스 표시
- 키보드만으로 메뉴와 버튼 사용 가능
- 문서 언어에 맞는 `lang` 속성 지정
- 정보 이미지에는 대체 텍스트를 제공하고 장식 이미지는 빈 대체 텍스트 사용
- `prefers-reduced-motion` 설정 존중
- 지나친 애니메이션과 무거운 동영상 배경은 사용하지 않음

공식 디자인 자료가 없다면 우선 흑백·중립색 기반 와이어프레임을 만든 뒤, 승인된 색상·로고·이미지를 적용한다.

## 8. 검색 및 공유 설정

- 페이지별 한국어·영어 제목과 설명 설정
- 페이지별 canonical URL 설정
- 한국어·영어 페이지를 연결하는 `hreflang`과 `x-default` 설정
- 카카오톡·SNS 공유용 Open Graph 이미지 제작
- 언어별 Open Graph locale 설정
- 파비콘 설정
- 행사명, 날짜, 장소가 검색 결과에 나타나도록 메타데이터 작성
- 한국어·영어 페이지의 언어 정보 지정
- 존재하지 않는 주소를 위한 404 페이지 제작
- 기본 `github.io` HTTPS 사용

방문 통계는 기본적으로 넣지 않는다. 실제 운영상 필요할 때만 Google Analytics 등 분석 도구를 추가하고 관련 안내를 검토한다.

## 9. 협업 방식

### 9.1 작업 원칙

- `main`은 항상 공개 가능한 상태로 유지한다.
- 모든 변경은 작업용 브랜치에서 진행한다.
- Pull Request를 통해 검토한 뒤 `main`에 병합한다.
- `main` 병합 후 GitHub Actions가 사이트를 자동 배포한다.

브랜치 이름 예시는 다음과 같다.

- `setup/astro`
- `design/homepage`
- `content/ko-copy`
- `content/en-copy`
- `content/schedule`
- `fix/mobile-layout`

### 9.2 작업 흐름

1. GitHub Issue에 작업 내용과 담당자를 기록한다.
2. 해당 Issue에 맞는 브랜치를 만든다.
3. Codex를 이용해 코드나 콘텐츠를 수정한다.
4. 로컬 미리보기로 결과를 확인한다.
5. 변경 사항을 커밋하고 GitHub에 푸시한다.
6. Pull Request를 생성한다.
7. 다른 운영진 최소 1명이 문구·링크·화면을 확인한다.
8. 승인 후 `main`에 병합한다.
9. 자동 배포가 성공했는지 실제 주소에서 확인한다.

### 9.3 보호 설정

`main` 브랜치에는 다음 규칙을 적용한다.

- 직접 푸시 제한
- Pull Request 필수
- 승인 1명 이상 필요
- 강제 푸시 금지
- 보호된 `main` 브랜치 삭제 금지
- `npm run build` 성공 후에만 병합 허용

GitHub Actions는 Pull Request에서 빌드 검사를 실행하고, `main` 병합 후에만 Pages 배포를 실행한다. 브랜치 보호의 필수 상태 검사는 Pull Request 빌드 작업으로 지정한다.

긴급 수정도 별도 브랜치와 Pull Request를 사용하는 것을 원칙으로 한다.

## 10. 개발 단계

### 1단계: 운영 기반과 필수 정보 확정

- `qff-ku` Organization 이름은 생성 화면에서 최종 확인
- Organization 생성, Owner 2명 지정
- `qff-ku.github.io` 공개 저장소 생성
- 브랜치 보호와 Pages 권한 설정
- 공식 행사명, 날짜, 장소, 주최·주관, 공개일 확정
- 한국어 원문과 영어 번역의 승인자 지정

나머지 프로그램·연사 정보는 확정 여부를 구분해 `추후 공개`로 시작할 수 있다. 미확정 내용을 임의로 채우지 않는다.

### 2단계: 기본 프로젝트와 첫 배포

- Astro 프로젝트와 최소 공통 구조 생성
- `site`, `trailingSlash`, 루트 리디렉션 설정
- 2026 콘텐츠 데이터와 행사 상태 생성
- 공식 Astro GitHub Action으로 자동 배포 설정
- Pull Request에서는 빌드만, `main`에서는 빌드 후 배포하도록 워크플로 구성
- 패키지 lockfile 커밋
- 저장소 Pages 배포 소스를 `GitHub Actions`로 설정
- 임시 콘텐츠로 실제 주소의 첫 배포 확인

### 3단계: 공통 화면과 한국어 페이지

- 모바일 우선으로 한 페이지형 화면 구현
- 헤더·메인·소개·프로그램·참가·장소·FAQ·후원·푸터 구현
- 행사 상태에 따른 신청 버튼과 문구 구현
- 데스크톱 반응형 화면과 기본 접근성 적용

### 4단계: 영어와 실제 콘텐츠 적용

- 승인된 한국어 문구와 영어 번역 입력
- 일정과 연사 정보 입력
- 로고와 사진을 최적화해 적용
- 언어 전환, 지도·신청·SNS·문의 링크 연결
- 언어별 메타데이터와 공유 이미지 적용

### 5단계: 검수, 공개와 운영

- `npm run build` 성공 확인
- 모바일, 태블릿, 데스크톱과 Chrome, Edge, Safari 확인
- 한국어·영어 내용과 날짜·시간·장소·링크·이메일 대조
- 이미지 저작권, 로고 승인, 키보드 탐색, 포커스, 색상 대비 확인
- 이미지 용량, 404 페이지, SNS 공유 화면 확인
- 승인된 Pull Request를 병합하고 실제 주소에서 배포 확인
- 변경 사항은 `data/2026.ts` 수정 후 재배포
- 행사 종료 시 상태를 `ended`로 바꾸고 2026 페이지는 기록으로 유지
- 다음 행사가 열리면 `2027` 데이터와 페이지를 추가하고 루트 리디렉션만 변경

## 11. Codex 작업 단위

Codex 작업은 다음 네 단위로 진행한다.

1. 저장소, Astro 최소 구조, GitHub Pages 시험 배포
2. 공통 `EventPage`와 2026 한국어 페이지 구현
3. 영어 콘텐츠, 실제 이미지와 외부 링크 적용
4. 모바일·접근성·메타데이터 검수와 README 인수인계 작성

각 작업 요청에는 변경 범위, 사용할 공식 문구, 참고 디자인, 완료 기준을 함께 제공한다. Codex가 임의로 행사 정보나 주최 기관 명칭을 만들어 넣지 않도록 한다.

## 12. 필요한 사전 자료

다음 자료를 개발과 공개 과정에서 한 폴더에 정리한다. 공식 행사명, 날짜, 장소, 공개일, 승인자와 운영 담당자는 1차 개발 전에 필요하고 프로그램·연사·이미지는 확정되는 순서대로 적용할 수 있다.

- Qiskit Fall Fest 공식 영문·국문 명칭
- 2026 행사 주제와 소개문
- 확정 날짜·시간·장소
- 세부 프로그램
- 연사 이름, 소속, 직함, 사진, 소개
- 참가 대상과 신청 방법
- Google Forms 주소
- 공식 문의 이메일
- 주최·주관·후원 기관의 정확한 표기
- 사용이 허가된 로고 원본
- 행사 키비주얼 및 사진
- 공식 색상과 글꼴 지침
- 한국어 문구의 승인 담당자
- 영어 번역의 검수 담당자
- 행사 정보 변경을 반영할 운영 담당자

## 13. 완료 기준

다음 조건을 모두 만족하면 1차 사이트를 완료한 것으로 본다.

- `https://qff-ku.github.io`에서 정상 접속된다.
- 한국어·영어 페이지가 모두 제공된다.
- 모바일과 데스크톱에서 주요 정보가 깨지지 않는다.
- 행사명, 날짜, 장소, 신청 방법, 문의처를 확인할 수 있다.
- 언어 전환과 모든 외부 링크가 정상 작동한다.
- `npm run build`가 오류 없이 완료된다.
- `main` 병합 시 자동 배포된다.
- 각 페이지의 `lang`, canonical, `hreflang`이 올바르다.
- 키보드로 주요 메뉴와 신청 링크를 사용할 수 있고 포커스 위치를 확인할 수 있다.
- 사이트의 신청 상태와 실제 외부 신청 폼 상태가 일치한다.
- Organization Owner가 최소 2명 지정되어 있다.
- 콘텐츠 수정과 인수인계 방법이 README에 기록되어 있다.
- 2027 페이지를 기존 2026 페이지를 삭제하지 않고 추가할 수 있다.

## 14. 현재 확정안

- 동아리 홍보 사이트는 이번 프로젝트에서 제외한다.
- Qiskit Fall Fest 2026 사이트만 우선 제작한다.
- 개인 GitHub 계정으로 작업하되 Qiskit Fall Fest Organization이 저장소를 소유한다.
- Organization과 사이트 주소는 `qff-ku`를 우선 사용한다.
- 무료 GitHub Pages를 사용한다.
- 공개 정적 사이트로 제작한다.
- Astro와 GitHub Actions를 사용한다.
- 한국어·영어 페이지를 별도 URL로 제공한다.
- 처음부터 `/2026/` 경로를 사용해 다음 연도 확장에 대비한다.
- 작업은 브랜치, Pull Request, 리뷰 방식으로 진행한다.

## 15. 개발 시작 전 최종 결정 사항

다음 항목은 실제 코드 작업을 시작하기 전에 결정해야 한다.

1. `qff-ku` Organization 이름의 실제 사용 가능 여부
2. 사이트의 공식 국문·영문 행사명
3. 주최·주관 기관의 정확한 표기
4. 공식 디자인 자료의 존재 여부
5. 참가 신청 기능의 필요 여부와 외부 신청 링크
6. 페이지 공개 희망일
7. 한국어 원문과 영어 번역의 최종 승인자
8. 행사 정보 변경과 신청 상태를 갱신할 운영 담당자
